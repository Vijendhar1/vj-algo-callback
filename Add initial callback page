<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TradeSmart OAuth Callback - vjalgo.online</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #f0f4f8 0%, #d9e2ec 100%);
      color: #333;
      margin: 0;
      padding: 0;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }
    .container {
      background: white;
      padding: 40px;
      border-radius: 12px;
      box-shadow: 0 8px 30px rgba(0, 0, 0, 0.12);
      max-width: 700px;
      width: 90%;
      text-align: center;
    }
    h1 {
      color: #0066cc;
      margin-bottom: 20px;
      font-size: 28px;
    }
    p {
      font-size: 16px;
      line-height: 1.6;
      margin-bottom: 20px;
    }
    pre {
      background: #f8f9fa;
      padding: 20px;
      border-radius: 8px;
      border: 1px solid #e0e0e0;
      text-align: left;
      white-space: pre-wrap;
      word-wrap: break-word;
      font-family: 'Consolas', 'Courier New', monospace;
      font-size: 15px;
      overflow-x: auto;
      max-height: 200px;
    }
    button {
      background: #0066cc;
      color: white;
      border: none;
      padding: 14px 32px;
      font-size: 16px;
      border-radius: 8px;
      cursor: pointer;
      transition: background 0.3s;
      margin-top: 15px;
    }
    button:hover {
      background: #0055aa;
    }
    #copyStatus {
      margin-top: 12px;
      color: #28a745;
      font-weight: bold;
      min-height: 20px;
    }
    .note {
      font-size: 14px;
      color: #666;
      margin-top: 30px;
      border-top: 1px solid #eee;
      padding-top: 20px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>TradeSmart OAuth Callback Received</h1>
    
    <p>After logging in on TradeSmart, you have been redirected here.</p>
    <p><strong>Copy the authorization code / request token</strong> from the browser's address bar (everything after <code>?code=</code> or <code>?request_token=</code>).</p>
    
    <pre id="params">Waiting for parameters... (if you see this directly, no redirect happened yet)</pre>
    
    <button onclick="copyParams()">Copy Parameters to Clipboard</button>
    <div id="copyStatus"></div>

    <div class="note">
      This is a secure, personal callback page owned by you for TradeSmart API integration.<br>
      Paste the copied code into your script to complete authentication.
    </div>
  </div>

  <script>
    // Get URL parameters (search + hash)
    const params = window.location.search + window.location.hash;
    
    // Display them
    document.getElementById("params").textContent = 
      params.length > 1 ? params : "No parameters found in URL (direct access or error)";

    function copyParams() {
      const textToCopy = params.length > 1 ? params : "";
      
      if (textToCopy) {
        navigator.clipboard.writeText(textToCopy).then(() => {
          document.getElementById("copyStatus").textContent = "Copied successfully!";
          setTimeout(() => {
            document.getElementById("copyStatus").textContent = "";
          }, 4000);
        }).catch(() => {
          document.getElementById("copyStatus").textContent = "Copy failed – please select and copy manually";
        });
      } else {
        document.getElementById("copyStatus").textContent = "Nothing to copy";
      }
    }
  </script>
</body>
</html>
