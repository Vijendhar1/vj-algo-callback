<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TradeSmart OAuth Callback - vjalgo.online</title>
  <style>
    body { font-family: Arial, sans-serif; padding: 40px; background: #f0f4f8; color: #333; max-width: 800px; margin: auto; text-align: center; }
    h1 { color: #0066cc; }
    pre { background: white; padding: 20px; border: 1px solid #ddd; border-radius: 8px; overflow: auto; white-space: pre-wrap; font-family: monospace; text-align: left; margin: 20px auto; max-width: 90%; }
    button { padding: 12px 24px; background: #0066cc; color: white; border: none; border-radius: 6px; cursor: pointer; font-size: 16px; margin-top: 10px; }
    button:hover { background: #0055aa; }
    #copyStatus { margin-top: 10px; color: green; font-weight: bold; }
  </style>
</head>
<body>
  <h1>TradeSmart OAuth Callback Received</h1>
  <p>After you log in on TradeSmart, you'll be redirected here with the auth code in the browser's address bar (URL).</p>
  <p><strong>Copy the full parameters below (or from the address bar):</strong></p>
  <pre id="params">Waiting for redirect... (refresh after redirect)</pre>
  
  <button onclick="copyParams()">Copy to Clipboard</button>
  <div id="copyStatus"></div>

  <script>
    const params = window.location.search + window.location.hash;
    document.getElementById("params").textContent = params || "No parameters detected (you accessed directly).";

    function copyParams() {
      navigator.clipboard.writeText(params).then(() => {
        document.getElementById("copyStatus").textContent = "Copied successfully!";
        setTimeout(() => { document.getElementById("copyStatus").textContent = ""; }, 4000);
      }).catch(() => {
        document.getElementById("copyStatus").textContent = "Copy failed – please select and copy manually.";
      });
    }
  </script>
</body>
</html>
